---
description: Switch to whisper.cpp small.en (~466MB, current default — balanced)
---

!CLAUDE_PLUGIN_DATA=${CLAUDE_PLUGIN_DATA} python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py whisper-model ggml-small.en.bin

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
