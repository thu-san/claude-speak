---
description: Set TTS playback rate (pitch-preserving)
argument-hint: "<0.25-4.0>  (e.g. 1.0 normal, 1.3 brisk, 2.0 fast)"
---

!python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py rate $ARGUMENTS

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
