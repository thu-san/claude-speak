---
description: Show claude-speak daemon status (uptime, in-flight, pid)
---

!python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py daemon status

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
