---
description: Stop the claude-speak daemon (next speak request will respawn it unless daemon is off)
---

!python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py daemon stop

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
