---
description: Show current claude-speak config + tooling availability
---

!CLAUDE_PLUGIN_DATA=${CLAUDE_PLUGIN_DATA} python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py status

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
