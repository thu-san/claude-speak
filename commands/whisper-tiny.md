---
description: Switch to whisper.cpp tiny.en (~75MB, fastest, low accuracy)
---

!python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py whisper-model ggml-tiny.en.bin

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
