---
description: Toggle per-sentence/per-step verbose log lines
argument-hint: "<on | off>"
---

!python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py verbose $ARGUMENTS

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
