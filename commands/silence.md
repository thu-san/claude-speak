---
description: Seconds of silence after which dictation recording stops
argument-hint: "<0.3-10>  (e.g. 1.5 snappy, 4 default, 8 long pause)"
---

!python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py silence $ARGUMENTS

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
