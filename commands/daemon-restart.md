---
description: Restart the claude-speak daemon (pick up new code / reload models)
---

!CLAUDE_PLUGIN_DATA=${CLAUDE_PLUGIN_DATA} python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py daemon restart

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
