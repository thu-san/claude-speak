---
description: Disable TTS playback (config only; no plugin uninstall)
---

!CLAUDE_PLUGIN_DATA=${CLAUDE_PLUGIN_DATA} python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py off

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
