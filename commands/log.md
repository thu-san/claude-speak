---
description: Tail the claude-speak log (last 40 lines by default; pass a number to override)
---

!python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py log $ARGUMENTS

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
