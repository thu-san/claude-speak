---
description: TTS playback mode — sentence-stream or single-buffer
argument-hint: "<stream | whole>  (stream = fast first audio; whole = smoother prosody)"
---

!CLAUDE_PLUGIN_DATA=${CLAUDE_PLUGIN_DATA} python3 ${CLAUDE_PLUGIN_ROOT}/scripts/speak_config.py mode $ARGUMENTS

Print the stdout from the command above verbatim inside a fenced code block. No commentary, no summary, no extra lines.
